var express = require('express'),
    app = express(),
    http = require('http').Server(app).listen(80);
console.log('Server Started');
app.get("/", function (req, res) {
    res.sendFile(__dirnamem + "/index.html")
})